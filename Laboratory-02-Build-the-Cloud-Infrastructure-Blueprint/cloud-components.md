# Cloud Infrastructure Components

## Compute Resources

The compute resource used in the laboratory is a virtual machine provided by the KillerCoda environment. The virtual machine runs with 1 virtual CPU and an Intel Xeon E312xx processor. Compute resources are important in cloud computing because they provide the processing capacity needed to run applications, services, and operating system processes. In this laboratory, the virtual machine provides the computing environment where Linux commands and cloud infrastructure activities are performed.

## Storage Resources

The main storage resource detected in the laboratory environment is the `/dev/vda1` filesystem. It has a total size of 19 GB, with 5.4 GB used and 13 GB available. Storage resources are important in cloud computing because they provide space for the operating system, applications, configuration files, and other data. In the KillerCoda environment, the virtual storage supports the Ubuntu operating system and laboratory activities.

## Networking Resources

The main network interface detected is `enp1s0`, with the IPv4 address `172.30.1.2/24`. A Docker virtual interface named `docker0` also exists with the address `172.17.0.1`, but it is not used as the main server IP for this laboratory. Networking resources are important in cloud computing because they allow systems and services to communicate with users and other resources. In this laboratory, the network interface provides connectivity for the Ubuntu virtual machine.

## Operating System

The operating system used in the laboratory is Ubuntu 24.04.4 LTS. The system uses the Linux kernel version `6.8.0-136-generic` and the x86_64 architecture. The operating system is important because it manages the virtual machine's hardware resources, files, processes, and network services. In this laboratory, Ubuntu provides the Linux environment where the cloud infrastructure commands and activities are executed.
